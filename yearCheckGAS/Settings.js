/**
 * ==============================================================================
 * Google Apps Script - 장기요양기관 케어포 연동 및 서류 관리 자동화 (Settings)
 * ==============================================================================
 * 프로젝트 전역에서 공통으로 사용되는 색상 코드, 시트 명칭, 시스템 제어용
 * 설정값들을 관리하는 모듈입니다.
 *
 * [주요 역할]
 * - 수급자 상태 및 오류 여부를 시각적으로 구분하기 위한 UI 색상 팔레트 정의
 * - 데이터 연동 및 로그 출력을 위한 기준 시트 이름 고정
 * - 다중 실행 방지용 스크립트 프로퍼티 키 관리
 */

// UI 색상 설정 (상태별 시각적 구분)
const COLOR_POSITIVE = "#d9ead3"; // 긍정 상태 (수급중, 정상작성, 연동성공)
const COLOR_NEGATIVE = "#f4ccd0"; // 부정 상태 (미작성, 오류, 갱신필요)
const COLOR_NEUTRAL = "#cccccc"; // 중립 상태 (보류, 계약해지, 해당없음)
const COLOR_WHITE = "#ffffff"; // 기본 배경색 (특이사항 없음)
const COLOR_TITLE = "#34495e"; // 테이블 헤더 및 타이틀 강조색

// 시스템 및 시트 명칭 설정
const SHEET_NAME_CAREFOR = "케어포연동"; // 원본 데이터가 위치하는 탭 이름
const SHEET_NAME_LOG = "Log"; // 에러 및 경고 내역을 기록하는 탭 이름
const PROP_KEY_RUNNING = "CAREFOR_PIPELINE_RUNNING"; // 동시 실행 방지를 위한 상태 값 키
